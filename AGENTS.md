# 수행도우미 개발 가이드

이 문서는 `dy6553/wanhee` 저장소에서 기능 추가, UI 수정, AI 라우팅, 배포 작업을 수행할 때 지켜야 할 프로젝트 기준을 정리한다.

## 1. 제품 목표

수행도우미는 초·중·고 학생이 수행평가의 과제 조건을 입력하면 주제 추천, 요구사항 분석, 초안 작성, 독립 검증까지 이어서 수행할 수 있게 돕는 웹 앱이다.

핵심 원칙은 다음과 같다.

- 교사 안내문과 루브릭을 가장 높은 우선순위로 반영한다.
- 과목, 학교급, 학년, 수행평가 형식, 난이도에 맞춰 결과를 조절한다.
- 확인되지 않은 사실이나 성취기준 코드를 임의로 확정하지 않는다.
- 긴 결과를 한 화면에 누적하지 않고, 결과 화면을 분리해 모바일 접근성을 높인다.
- 사용자가 시작한 AI 작업은 진행률과 예상 시간을 확인할 수 있어야 한다.
- 최근 작업 기록은 최대 24시간 유지하고 이후 자동 삭제한다.

## 2. UI/UX 기준

- 모바일 우선으로 설계한다.
- 불필요한 카드, 과도한 둥근 모서리, 큰 상하 여백을 줄인다.
- 결과물은 ChatGPT 대화 화면처럼 본문 중심으로 읽기 편해야 한다.
- 긴 결과는 `작성 전략 -> 초안 -> 검증 결과`처럼 단계별 별도 화면으로 이동한다.
- 이전/다음 행동은 모바일에서 쉽게 접근할 수 있어야 한다.
- 주요 실행 버튼은 화면 하단 또는 결과 바로 다음 위치에서 즉시 접근 가능해야 한다.
- 진행 중인 AI 요청은 퍼센트와 예상 남은 시간을 표시한다.
- 오류 메시지는 `Failed to fetch` 같은 원문보다 사용자가 이해할 수 있는 한국어 안내를 우선한다.

## 3. AI 모델 선택

AI 모델은 사용자가 직접 고르지 않아도 되도록 자동 라우팅한다.

라우터는 최소한 다음 문맥을 고려한다.

- 작업 단계: 주제 추천, 분석, 작성, 검증, 최종 재작성
- 과목
- 학교급 및 학년
- 수행평가 형식
- 입력 길이
- 난이도 1~7단계
- 빠른 응답 모드 여부

Supabase Model Registry의 승인 모델만 사용한다. 학생 데이터 사용이 허용되고, 개인정보/보안 검토 및 production 승인을 통과한 모델만 후보로 허용한다.

새 NVIDIA 모델은 카탈로그 동기화 후 바로 학생 데이터에 사용하지 않고 승인 절차를 거친 뒤 후보가 된다.

## 4. 사실성 및 검증

- 최신 통계, 법, 정책, 연구 결과, 현재 상황은 외부 검증 없이는 확정 표현하지 않는다.
- 출처가 필요한 항목은 `sourceNeeds`에 남긴다.
- 불확실한 항목은 `uncertainties`에 남긴다.
- 독립 검증 결과에서 외부 확인이 필요한 사실은 `NEEDS_WEB_VERIFICATION`으로 표시한다.
- 구조화 AI 응답은 Zod 스키마 계약을 반드시 만족해야 한다.

## 5. 기록 및 작업 유지

- 최근 수행평가 작업 기록은 24시간만 유지한다.
- 분석, 초안, 검증 상태와 결과를 기록에서 확인할 수 있어야 한다.
- 24시간이 지나면 자동 삭제한다.
- 브라우저나 운영체제가 프로세스를 완전히 종료하면 클라이언트 기반 백그라운드 작업은 보장되지 않는다는 점을 과도하게 숨기지 않는다.

## 6. 배포 절차

앱/코드 변경은 다음 순서로 마무리한다.

1. GitHub `main`에 반영한다.
2. Vercel production 배포가 생성되는지 확인한다.
3. 빌드 오류가 없는지 확인한다.
4. 최종 deployment가 `READY`인지 확인한다.
5. 필요하면 runtime log에서 새 오류가 없는지 확인한다.
6. 최종 배포 상태를 확인하기 전에는 사용자에게 배포 완료라고 말하지 않는다.

문서만 변경해 앱 런타임에 영향이 없는 경우에도 Git 연동으로 production deployment가 생성되면 상태를 확인한다.

## 7. 변경 안전성

- 큰 파일 전체 교체 전에는 현재 파일을 다시 읽고 최신 SHA를 기준으로 수정한다.
- 동일 파일의 연속 업데이트는 직전 `content_sha`를 사용한다.
- 기존 기능을 제거하거나 흐름을 바꾸는 변경은 이전 동작을 유지할 수 있는지 확인한다.
- 사용자 요청과 무관한 리팩터링을 동시에 섞지 않는다.

## 8. 주요 경로

- `src/features/assessment/assessment-wizard.tsx`: 수행평가 입력/흐름 UI
- `src/features/assessment/server/service.ts`: AI 프롬프트 및 수행평가 서버 로직
- `src/lib/ai/router.ts`: AI 모델 자동 라우팅
- `src/lib/ai/nvidia.ts`: NVIDIA 호출 및 구조화 출력 처리
- `src/components/ai-request-progress.tsx`: AI 진행률/예상 시간/알림
- `src/app/api/assignment/*`: 수행평가 API 라우트
- `public/sw.js`: 서비스 워커

이 문서는 구현과 실제 동작이 달라지면 함께 갱신한다.
